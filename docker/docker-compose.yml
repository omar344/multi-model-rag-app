version: "3.9"

services:
  mongo:
    image: mongo:8.0.6
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./mongodb:/data/db
    networks:
      - backend
    restart: always

networks:
  backend: